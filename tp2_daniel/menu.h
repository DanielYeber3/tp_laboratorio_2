
/** \brief Es el Menu principal.
 *
 * \param list[] sEmployee Recibe un array de la estructura.
 * \param len int Recibe el tamaño del array.
 * \return void No retorna nada.
 *
 */
void menuUno(sEmployee list[], int len);

/** \brief Es el Menu secundario de "Mostrar".
 *
 * \param list[] sEmployee Recibe un array de la estructura.
 * \param len int Recibe el tamaño del array.
 * \return void No retorna nada.
 *
 */
void menuDos(sEmployee list[], int len);
